# `soro_simulator_driver`

This package contains logic to run the Webots simulator. The `driver` Node provides fake info from the virtual world to emulate data coming from the Rover.

## Running

TODO
